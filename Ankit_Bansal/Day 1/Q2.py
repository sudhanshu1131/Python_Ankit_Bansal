# write a program which takes the name of the user as input and replace all the occurence of character 'a' in the name to 'b' and print it.

User_name = input("Enter your name: ")
modified_name = User_name.replace('a', 'b')
print(modified_name)